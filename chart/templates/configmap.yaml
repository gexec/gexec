apiVersion: v1
kind: ConfigMap

metadata:
  name: {{ include "gexec.fullname" . }}-providers
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "gexec.labels" . | nindent 4 }}

data:
  config.yaml: |
    providers:
      {{- toYaml .Values.auth.providers | nindent 6 }}
